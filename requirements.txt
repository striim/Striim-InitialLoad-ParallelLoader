cachetools==5.5.0
certifi==2024.12.14
charset-normalizer==3.4.1
google-api-core==2.24.0
google-auth==2.37.0
google-cloud-bigquery==3.27.0
google-cloud-core==2.4.1
google-crc32c==1.6.0
google-resumable-media==2.7.2
googleapis-common-protos==1.66.0
grpcio==1.69.0
grpcio-status==1.69.0
idna==3.10
packaging==24.2
psycopg2-binary
proto-plus==1.25.0
protobuf==5.29.3
pyasn1==0.6.1
pyasn1_modules==0.4.1
python-dateutil==2.9.0.post0
requests==2.32.3
rsa==4.9
six==1.17.0
tinydb==4.8.2
urllib3==2.3.0
# Splitter only (manage.sh split). Kept out of the base install because some
# PyPI mirrors don't carry it and it must never block the tests / state commands
# (it is lazy-imported). Install separately when you need splitting:
#   pip install python-oracledb
#
# Source-engine drivers (install only the engine(s) you probe/split against):
# psycopg2-binary   # Postgres source (already required above for the PG state backend)
# pyodbc            # SQL Server source (also needs an ODBC driver, e.g. "ODBC Driver 18 for SQL Server")
# JayDeBeApi        # Generic JDBC source (needs JPype1 + Java on PATH)
# JPype1            # Generic JDBC source
# python-oracledb   # Oracle source (thin mode; no Instant Client needed)
